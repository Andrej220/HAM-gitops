apiVersion: apps/v1
kind: Deployment
metadata:
  name: ham-deployment
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: datacollector
        env:
        - name: APP_STAGE
          value: "test"
        - name: DATACOLLECTOR_CONFIG_PATH
          value: /etc/ham/config.yaml
