apiVersion: apps/v1
kind: Deployment
metadata:
  name: ham-deployment
spec:
  replicas: 1
  template: 
     spec: 
       containers:
          - name: datacollector
            imagePullPolicy: Always
          - name: datacollectorProducer
            imagePullPolicy: Always

